export const enviroments = {
  dev: '.env', // Cuando el ambiente sea dev llama al archivo .env
  stag: '.stag.env', // Cuando el ambiente sea stag llama al archivo .stag.env
  prod: '.prod.env', // Cuando el ambiente sea prod llama al archivo .prod.env
};
